<%= simple_form_for([@trip, @hotel_booking]) do |f| %>
  <%= f.input :date_coming %>
  <%= f.input :date_leaving %>
  <%= f.association :hotel %>
  <%= f.button :submit %>
  <%= link_to "Delete Reservation" , hotel_booking_path(), method: :delete, data: { confirm: "Are you sure?" }%>
<%end%>

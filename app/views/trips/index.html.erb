<% @trips.each do |trip|%>
  <p><%= link_to trip.destination, trip_path(trip) %></p>
  <p><%= trip.total_price %></p>
  <p><%= trip.date_coming %></p>
  <p><%= trip.date_leaving %></p>
  <p><%= trip.type_transportation %></p>
  <p><%= trip.people %></p>
  <br>
<% end %>
